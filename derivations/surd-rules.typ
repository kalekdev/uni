For $a, b > 0$:
$
  sqrt(a) sqrt(b)     &= sqrt(a b)\
  (sqrt(a) sqrt(b))^2 &= sqrt(a)sqrt(a)sqrt(b)sqrt(b)\
                      &= a b\ \
  therefore sqrt(a b) &= sqrt(a) sqrt(b)\
$
\
$
  sqrt(a)/sqrt(b)     &= sqrt(a/b)\
  (sqrt(a)/sqrt(b))^2 &= a/b\

  therefore sqrt(a/b) &= sqrt(a)/sqrt(b)
$
