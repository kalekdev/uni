= Week 1 Bonusaufgaben

1. i. $
    N = integral.triple_V rho space d V = rho V
  $
  ii. $
    rho &= 10^28 m^(-3)\
    V   &= 61.3 times (10^(-2) m)^3 = 61.3 times 10^(-6) m^3\
    N   &= 61.3 times 10^(-6) times 10^28\
        &= 61.3 times 10^22 "Ladungstraeger"
  $

2. Antwort: *c*

3. i. Zylinderkoordinaten $
    r   &= sqrt(3^2+5^2) = sqrt(34)\
    phi &= arctan(5/3) = 1.03 "rad"\
    z = 2
  $
  ii. Kugelkoordinaten $
    2                &= r cos(theta.alt)\
    3                &= r sin(theta.alt) cos(phi)\
    5                &= r sin(theta.alt) sin(phi)\
    3/cos(phi)       &= 5/sin(phi)\
    tan(phi)         &= 5/3\
    phi              &= arctan(5/3) = 1.03 "rad"\
    2/cos(theta.alt) &= 3/(sin(theta.alt)cos(phi))\
    tan(theta.alt)   &= 3/(2cos(phi))\
    theta.alt        &= arctan(3/(2cos(phi))) = 1.24 "rad"
  $

4. $
    r   &= "constant"\
    F   &:= Q/(4 pi r^2) arrow(e_r)\
    Psi &:= integral_0^(2 pi) integral_0^pi r^2sin(theta.alt) arrow(F(r, theta.alt, phi )) dot d theta.alt d phi\
        &= (Q r^2)/(4pi r^2) integral_0^(2 pi) integral_0^pi sin(theta.alt) arrow(e_r) dot d theta.alt d phi\
        &arrow(e_r) dot d theta.alt d phi "is 1, as they are always parallel."\
        &= (Q)/(4pi) integral_0^(2 pi) (-cos(pi)) - (-cos(0)) d phi =
    (Q r^2)/(4pi r^2) integral_0^(2 pi) 2 d phi\
        &= (Q r^2)/(4pi r^2) ((4pi) - (0)) = (4 pi Q)/(4pi) = Q
  $

